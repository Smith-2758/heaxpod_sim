# 六足机器人三种地形实现说明

## 一、先说明当前代码结构

目前项目里并不是所有地形都走同一条执行链路。

斜坡和台阶场景仍然采用离线轨迹生成的方式。一般流程是：先运行 `MAIN/6leg_motion` 下对应的轨迹脚本，生成 `.mat` 文件；再由 `PG.m` 读取这些轨迹；最后交给 `CoppeliaSim_process.m` 在仿真中逐帧回放。

深沟场景则同时保留了两套方案。旧方案仍然挂在 `PG.m` 里，通过 `pattern = {'ditch'}` 读取 `walk_ditch.mat` 做开环回放。新方案在 `MAIN/6leg_motion/ditch` 目录下，先由 `walk_ditch.m` 生成基准轨迹 `xyz_base.mat`，再直接运行 `CoppeliaSim_learn_ditch.m` 做半闭环控制。这条新链路目前还没有并进 `main_v2 -> PG -> CoppeliaSim_process` 的主流程，所以文档里需要把两者分开看。

## 二、坐标对齐原则

斜坡和台阶这两类离线轨迹，对初始 X 坐标比较敏感。轨迹脚本里的 `body_x` 最好和 `CoppeliaSim_process.m` 里对应模式的 `position(1)` 保持一致，否则机器人在仿真中的落脚点会和场景前沿错开。

当前代码里，`slope` 和 `climb2wall` 两种模式在 `CoppeliaSim_process.m` 中都把机体初始位置设成了 `x = 3.8`，对应的 `walk_slope.m` 和 `walk3step_high.m` 里也都用了 `body_x = 3.8`。这一点是对齐的。

深沟半闭环脚本不走这套调度流程，它在 `CoppeliaSim_learn_ditch.m` 中直接用 `xb(1)`、`yb(1)` 设置机体初始位置，所以不能简单套用 `PG.m` 的那套说明。

## 三、15° 斜坡方案

斜坡场景由 `MAIN/6leg_motion/walk_slope.m` 生成轨迹，对应仿真模式是 `pattern = {'slope'}`。

代码里可以确认的核心做法有几项。第一，机器人起始 `body_x` 调整到了 `3.8`，和 `CoppeliaSim_process.m` 中 `case 'slope'` 的初始位置一致。第二，斜坡前沿坐标设为 `slope_start_x = 5.975`。第三，机体抬升不是等重心到达坡底才开始，而是从 `slope_start_x - 1.75` 开始触发，这个 1.75 m 对应的是机体中心到前端的大致距离。等机体尾部也基本上坡后，再过渡到完整姿态，也就是 `slope_start_x + 1.75` 这一段。

机体俯仰角和重心抬升系数都用了同一套平滑函数 `3p^2 - 2p^3`。俯仰角从 0 逐步过渡到 15°，高度折减系数则从 `0.2` 过渡到 `0.7`。足端部分没有再额外强行加一个固定抬高量，而是按各腿当前的 X 坐标，直接叠加坡面对应的高度。

逆运动学阶段，脚本会根据 `pitch0` 生成绕 Y 轴的旋转矩阵，并把这个姿态写到 `robot(1).R` 中，让求出来的关节角本身就包含机体贴坡的姿态变化。最后保存的文件是 `MAIN/6leg_motion/export_data/walk_slope.mat`，其中除了 `joint` 外，还带了 `xb`、`zb` 和 `pitch0`。`PG.m` 在 `case 'slope'` 中会读取这个文件，并把 `pitch0` 一并带出来。

## 四、0.5 m 台阶方案

台阶场景对应的轨迹脚本是 `MAIN/6leg_motion/walk3step_high.m`，在主流程里通过 `pattern = {'climb2wall'}` 调用。

从代码来看，这个版本主要改了三件事。第一，台阶前沿设为 `dais = 5.975`，台阶高度 `wh = 0.5`。第二，机体抬升不是线性硬拉，而是从 `dais - 1.5` 开始，到 `dais + 2.2` 才完成，抬升曲线同样用了 `3p^2 - 2p^3`。第三，机体最高点没有抬到完整的 0.5 m，而是限制在 `wh * 0.8`，也就是 0.4 m，这样可以减轻后腿悬空的问题。

足端高度的处理也比较直接。只要某条腿的 `x >= dais`，就把支撑面高度加上 `wh`；如果脚还没有越过台阶，但已经接近边缘，则额外抬高 0.1 m 来避免踢碰。整个脚本算完后会保存 `walk3step_high.mat`，由 `PG.m` 中的 `case 'climb2wall'` 读取，再交给 `CoppeliaSim_process.m` 执行。

另外，`CoppeliaSim_process.m` 现在已经会在回放过程中记录 `realX_log`、`realY_log` 和 `realZ_log`，仿真结束后还能输出受力图、轨迹图、速度图以及对应的 `.fig` 文件，所以台阶和斜坡场景都可以直接做轨迹对比。

## 五、深沟方案

### 1. 旧的开环回放链路

旧链路仍然保留在主程序中。`main_v2.m` 如果设置 `pattern = {'ditch'}`，会先调用 `PG.m`；`PG.m` 在 `case 'ditch'` 中读取的是 `MAIN/6leg_motion/export_data/walk_ditch.mat`；随后 `CoppeliaSim_process.m` 再按 `case 'ditch'` 中的初始位置 `[ -0.5; 0; 2.7 ]` 回放这条轨迹。

这条链路适合做已有轨迹的重复播放，但它本身还是开环的，不能代表现在正在试验的半闭环版本。

### 2. 新的半闭环试验链路

新的深沟方案在 `MAIN/6leg_motion/ditch` 目录下，分成两个脚本。

`walk_ditch.m` 负责生成最基础的波浪步态基准轨迹，并保存为 `xyz_base.mat`。这个脚本没有直接输出关节角，而是输出足端和机体的参考轨迹。它把所有支撑相的足端高度都设成 `zf0 - probe_depth`，其中 `probe_depth = 0.10`，也就是落脚时默认往地下再探 10 cm，用来给后续的物理探测留出空间。波浪步态顺序写得也比较明确，是 `3 -> 6 -> 2 -> 5 -> 1 -> 4`。

`CoppeliaSim_learn_ditch.m` 负责在线判断和动作修正。这个脚本会读取 `xyz_base.mat`，自己连接 CoppeliaSim，自己做逆运动学，也自己保存学到的结果 `walk_ditch_learned.mat`。因此它并不经过 `PG.m`，也不依赖 `CoppeliaSim_process.m`。

### 3. 当前半闭环版本里已经实现的机制

当前 `CoppeliaSim_learn_ditch.m` 里能明确看到的机制有以下几项。

第一，支撑相锁定。程序用 `z_locked` 和 `locked_z_val` 维护足端的支撑状态。只要足端仍处在支撑相，就优先保持锁定高度，不立刻回到理论轨迹。等到脚本判断该腿进入摆动相，且 `z0(leg, kk) >= zf0 + 0.05` 时，才解除锁定，并用 15 帧余弦插值做一个软过渡，避免起步时的高度突变。

第二，探坑判断。当前代码不是简单的延时确认，而是“短路触发 + 延后确认”一起用。当 `z0(leg, kk) < zf0 - 0.098` 且该腿不在锁定状态时，程序开始累计 `probe_wait`。如果在这个窗口内检测到 `real_foot_z < 0.98`，就立刻认为踩空，直接进入避险流程；如果一直没有踩空，等 `probe_wait > 30` 后再确认踩实，并重新锁定。

第三，支撑相滑落监测。程序会持续读取每条腿的力传感器值。如果某条腿在支撑期内连续 20 帧受力低于 `10 N`，就认为脚下已经滑脱，也会转入避险状态。

第四，避险动作已经改成“先直接前跨，失败再退回”。当前实现里每条腿都有独立的 `recover_state`。状态 1 是直接前跨试探，当前步长设为 `0.75 m`；如果这一步没有踩实，就进入状态 2，向后退回 `0.8 m` 左右的安全区；之后状态 3 再进行二次跨越，步长设为 `0.85 m`；如果二次跨越后仍不稳定，就进入状态 4，执行最终止损退回。

第五，单腿脱困。当前脚本把 `1.5 ~ 2.2 m` 作为坑中区间。只要某条腿已经踩实，但足端实际 X 仍落在这个区间内，就不会直接认定为过坑成功，而是进入 `recover_state = 5` 做单腿脱困。若是当前这条腿自己踩实后仍在坑中区间，则使用 `0.55 m` 的补跨；如果是另一条腿踩实后顺带检查到其他稳定腿还在坑中区间，则对那条腿触发较小的 `0.30 m` 补跨。两种情况下都会保留抬腿和再探测动作，但单次事件的脱困尝试次数仍限制为 1 次。

第六，偏航补偿和机体前移。程序每隔 500 帧读取一次机体 Yaw，并用 `delta_step_compensation = -1.5 * actual_yaw` 去调整左右步幅，单次补偿量限制在 `[-0.10, 0.10]`。当前版本仍然保留这一补偿，不再设置小角度死区。重心前后移动也已经不再是单段策略，而是按腿的确认情况分级推进：前腿尚未确认时先后移 `0.15 m`；前腿未全部确认前保持中性；两条前腿都确认后前移 `0.20 m`；两条中腿也确认后再前移 `0.20 m`，总计达到 `0.40 m`。此外，前腿和中腿都加入了“两级确认”机制，当它们稳定越过 `2.4 m` 后会转入长期确认，避免在明显过坑后反复回退判断。

第七，重心阶段防回跳和退出锁存。为了避免阶段 2 和阶段 3 来回切换，当前脚本规定在同一次过坑过程中，重心阶段只允许前进，不再从 3 回退到 2。同时又增加了 `cog_control_finished` 锁存，一旦本次过坑已经越过主控制区间并完成退出，就不再因为机身后续的小幅回摆重新进入重心补偿逻辑。

第八，步态与物理探测同步。只要存在任意一条腿仍在探测，或者仍在执行避险动作，主时钟 `kk` 就不会继续推进。这个处理能避免数学轨迹已经往前走了，但物理腿部动作还没完成的情况。

### 4. 目前还没完全并入主流程的地方

从现有代码看，深沟半闭环方案还处在单独试验脚本阶段。`main_v2.m` 当前虽然默认写的是 `pattern = {'ditch'}`，但这条命令实际调用的仍然是旧的 `walk_ditch.mat` 开环回放，而不是新的 `CoppeliaSim_learn_ditch.m`。

另外，`CoppeliaSim_process.m` 的结果归档功能比 `CoppeliaSim_learn_ditch.m` 更完整，会自动输出 4 张图和 `simulation_summary.md`。而当前版本的 `CoppeliaSim_learn_ditch.m` 收尾部分还比较简单，主要保存的是学习后的关节轨迹和基础图像。如果后面打算长期保留这条链路，最好把它也整理成和主流程一致的输出格式。

## 六、建议的运行方式

如果目的是复现实验结果，建议按下面的方式执行。

斜坡场景：先运行 `MAIN/6leg_motion/walk_slope.m` 更新 `walk_slope.mat`，然后在 `main_v2.m` 中设置 `pattern = {'slope'}`，再运行 `main_v2.m`。

台阶场景：先运行 `MAIN/6leg_motion/walk3step_high.m` 更新 `walk3step_high.mat`，然后在 `main_v2.m` 中设置 `pattern = {'climb2wall'}`，再运行 `main_v2.m`。

深沟旧方案：确认 `MAIN/6leg_motion/export_data/walk_ditch.mat` 已经准备好，再在 `main_v2.m` 中设置 `pattern = {'ditch'}` 运行。

深沟新方案：先运行 `MAIN/6leg_motion/ditch/walk_ditch.m` 生成 `xyz_base.mat`，再直接运行 `MAIN/6leg_motion/ditch/CoppeliaSim_learn_ditch.m`。这一步不经过 `main_v2.m`。

## 七、当前仍存在的问题

虽然目前的半闭环深沟方案已经能完成基本跨坑，但从最近几轮实际仿真来看，仍有几个问题没有解决。

第一，仿真速度过慢。按目前脚本的运行情况，几十秒的理论仿真往往需要二十多分钟才能跑完。原因大致与同步步进、频繁读取物理传感器、图表记录较多，以及 `kk` 在探测和避险阶段经常被冻结有关。这个问题不会直接改变动作结果，但明显拉长了调参周期。

第二，最开始设计的重心后移没有完全达到预期。当前代码虽然已经在前腿未确认时加入了 `-0.15 m` 的后移阶段，但从实际表现看，机器人在最初接近坑边时并没有明显把重心压住，因此这部分还需要继续观察和调整。

第三，也是目前更关键的问题，是机器人伸腿入坑的能力仍然不稳定。现在很多情况下，机器人只有在姿态已经有一点失稳时，前腿才比较容易真正伸进坑中；如果机身状态过于“平稳”，足端反而不容易自然探入坑内。这说明当前的伸腿方式、姿态约束和重心配合之间还没有完全理顺。

## 八、当前文档结论

如果只按现在仓库里的代码来判断，可以把项目理解成这样：斜坡和台阶已经并入了标准离线回放流程；深沟的新半闭环方案已经有一套比较完整的试验脚本，但还没有正式接到主程序里。这样写虽然没那么“完整”，但和代码是一致的，也更接近目前项目的真实状态。
