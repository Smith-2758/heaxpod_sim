# 六足机器人复杂地形穿梭研究日志

## 一、仿真环境配置（2026-03-14）
为了测试六足机器人在复杂地形下的开环跨越能力，我在 CoppeliaSim 中搭建了三个场景，分别是 15° 斜坡、50 cm 深沟和阶梯障碍。斜坡场景主要用于观察重心变化和爬坡姿态，深沟场景用来检查步态切换时是否会踩空，阶梯场景则用于评估抬腿高度和机体通过性的上限。目前 `6leg_slope.ttt`、`6leg_ditch.ttt` 等场景文件的模型和物理参数已经设置完成。

## 二、15° 斜坡跨越算法优化（2026-03-15 至 2026-03-16）
前期做开环测试时，机器人在上坡初段容易出现机头碰撞、后腿打滑和重心不稳的问题。这两天主要围绕这几个现象调整了控制代码。

### 1. 提前抬升机体，避免入坡碰撞
原来的程序是等机体中心走到坡底后才开始抬升，但这种写法没有把前半段机身长度考虑进去。机器人的总长约为 3.5 m，头部相对机体中心前伸约 1.75 m。根据这个尺寸，我将 Z 轴抬升的触发位置提前到坡前 1.75 m 处。这样机器人头部刚接触坡面时，机体就已经开始抬升，入坡时的碰撞现象明显减少。

### 2. 重心高度改为分段调整
入坡初期如果机体抬得过高，后腿容易打滑。为了解决这个问题，我增加了一个随位移变化的动态高度系数 `coeff_bb`。机器人刚进入斜坡时，先将系数限制在 0.2，使重心尽量贴近地面；等机体基本爬上坡面后，再逐步过渡到 0.7，以保证底盘有足够的离地间隙。这样处理后，抓地性和通过性都比之前稳定一些。

### 3. 用逆运动学计算俯仰姿态
之前我直接通过 API 修改机体俯仰角，这种做法和仿真中的碰撞约束不够一致，容易产生不自然的姿态。现在的做法是先计算期望俯仰角，再将其写成旋转矩阵 `Ry(theta)`，然后送入 MATLAB 的逆运动学求解循环，由程序给出各关节在斜坡上的实际角度。改完以后，机体贴合坡面的效果更自然，各腿受力也更均匀。

### 4. 用平滑曲线替代突变补偿
原先的高度补偿是在某一步突然加入的，因此仿真中机体会出现明显的跳变。现在我改用贝塞尔曲线处理高度和俯仰角的过渡，把原本的突变过程拉成连续变化。调整后，机体因高度变化过快而产生的上下振荡明显减小。

### 5. 增加实际轨迹对比
仅靠肉眼观察，很难准确判断改动前后的差异。因此我在主循环中增加了对引擎质心坐标 `body_p` 的读取，并将设定轨迹与实际轨迹放在同一张图中进行对比。这样可以直接看到机器人在斜坡上的打滑距离，也给后续调参提供了更明确的依据。

## 三、0.5 m 台阶盲走跨越算法修复与优化（2026-03-15 至 2026-03-18）
此前台阶场景中保留了一部分基于阈值的高度补偿旧逻辑，运行时会出现判断方向错误、机体抬升过急以及后腿悬空等问题。这几天主要针对这些现象做了修正。

### 1. 修正高度判断方向和重复补偿问题
原来的代码把上台阶写成了下台阶，结果是足端越过台阶边界后，高度反而降回去了，机器人在台阶前先抬起，过界后又突然下落。同时，台阶边缘还会同时触发基础高度补偿和避障额外抬高，使个别腿额外多升高 0.1 m。现在这两个问题都已修正，台阶前后的支撑面逻辑恢复正常。

### 2. 用平滑函数处理台阶抬升过程
原本机体质心的抬升过程是线性的，过渡比较生硬。我借鉴了斜坡场景中的处理方式，在机体 Z 轴高度 `zbb` 上引入了 `3p^2 - 2p^3` 的平滑过渡函数。这样机体靠近台阶时会先缓慢抬升，中段速度加快，接近目标高度后再逐步放缓。改完以后，台阶跨越过程中的高度振荡减轻了不少。

### 3. 调整最高点时机，避免后腿悬空
测试中发现，前两条腿刚踏上台阶时，如果机体立即整体升高 0.5 m，后腿还停留在平地上，就会因为拉伸过大而失去支撑，严重时会导致翻转或卡死。针对这个问题，我做了两点调整。一是将机体达到最高点的时间后移，推迟到位移越过台阶 2.2 m 之后，也就是等大部分腿已经上台阶后再抬到最高。二是将盲走模式下的最高底盘高度由 0.5 m 下调为 0.4 m，保留 10 cm 的余量来换取更稳定的后腿着地状态。

### 4. 将实际轨迹追踪扩展到全部地形
为了更方便地评估这些修改的效果，我把原来只在斜坡模式下启用的引擎追踪代码扩展到了全部地形。现在无论是斜坡、台阶还是深沟，仿真结束后都可以直接得到机器人质心 X/Z 方向的实际轨迹对比图。通过这些图可以更直观地看到，机体抬升策略调整后，整体轨迹比之前平顺很多。

## 四、仿真日志目录与图表归档增强（2026-03-20）
为了便于后续统计和复盘，今天对 `MAIN/CoppeliaSim_process.m` 中的结果归档逻辑做了统一整理。

### 1. 输出目录统一放到 `log` 根目录
仿真结果目录由原先的 `项目根目录/YYMMDD/HH.MM` 调整为 `项目根目录/log/YYMMDD/HH.MM`。例如：`D:\codehub\hexapod\轨迹仿真程序\log\260320\12.33`。如果在同一分钟内重复运行程序，系统会自动追加后缀，例如 `12.33_01`，以避免结果被覆盖。

### 2. 在 `png` 之外增加 `fig` 文件保存
原有流程会保存 4 张 `png` 图，包括受力、总力、轨迹和速度。现在在此基础上同步保存对应的 `.fig` 文件，后续可以直接在 MATLAB 中打开，继续修改标注、调整坐标轴范围，或者与其他实验结果做叠加对比。

### 3. 同步更新 Markdown 摘要文件
`simulation_summary.md` 中“输出文件”部分也做了同步更新，除了原有的 `PNG` 文件外，现在也会记录 `FIG` 文件列表。这样每次仿真结束后，图像文件和文字摘要能够保持一致，后续查找更方便。

## 五、深沟力觉半闭环跨越方案实现与结果记录（2026-03-21）
这几天我继续调试深沟跨越场景，将原来的纯开环方案改成了带力觉反馈的半闭环方案。目前机器人已经能够在仿真中完成跨坑动作，本次结果保存在 `MAIN/6leg_motion/log/260321/16.15_learn`。

### 1. 将跨坑控制拆分为轨迹生成和在线判断两部分
原有的跨坑方案基于 `walk3step.m`，主要在离线轨迹生成阶段通过 7 段 `for` 循环手动调整 `l_pit` 和 `l_pit0`，用几何补偿的方式跨越深沟。这种方法要求预先知道坑的位置，一旦落脚判断不准，就很容易失败。

这次我把跨坑控制拆成了两个脚本。`ditch/walk_ditch.m` 负责生成带 9 cm 探地深度的平地波浪步态基准轨迹，并保存为 `xyz_base.mat`；`ditch/CoppeliaSim_learn_ditch.m` 则在仿真运行期间实时读取力传感器和足端实际 Z 坐标，用来判断当前这条腿是踩实了还是落入深沟，再根据判断结果切换后续动作。这样处理后，控制逻辑比原来更清楚，也不再完全依赖预先设定的坑位。

### 2. 为每条腿加入三态反射机制
在深沟场景中，我为每条腿单独设计了一个三态反射机制。第一种状态是后退 160 帧，使用余弦曲线平滑后撤 0.3 m，让足端先回到安全区域。第二种状态是大步跨越 160 帧，向前跨出 0.8 m，再做一次着地探测。第三种状态用于二次探测仍然失败的情况，此时足端再后退 0.8 m，并停在原地等待步态收尾。

当天使用的探测逻辑仍然比较保守，主要依赖足端高度阈值和短时间延迟确认来判断是否踩实。这个版本能完成跨坑，但在坑边位置的响应还不够快，后面还需要继续改。

### 3. 修正初始锁定高度引起的大冲击
之前程序初始化时，将所有腿的 `z_locked` 都设成了 `z0`，也就是地面以下 9 cm 的探针深度。但在仿真刚开始时，足端实际还落在地面上，这样的初值和真实状态并不一致，因此预热阶段会出现接近 `10^5 N` 的冲击力。今天我把初始锁定高度改成了 `zf0`，即真实地面高度。修改后，初始化阶段的异常冲击已经消失。

### 4. 当前结果和后续待处理问题
从目前的仿真结果来看，机器人在遇到深沟后能够依次触发后退、大步跨越等动作，并最终完成跨坑。各腿受力图、总受力图、轨迹图、速度图以及对应的 `.fig` 文件都已经完成归档。

接下来还有两个问题需要继续处理。第一个是起步和抬腿时的瞬时冲击仍然偏大，可能与 `z_locked` 解锁瞬间的轨迹衔接不连续以及插值后的速度突变有关，后续需要在摆动相起步阶段加入更平缓的过渡。第二个是波浪步态长时间运行后会出现一定偏航，机器人在 Y 方向上难以保持直线，推测与左右腿摆动顺序不完全对称，以及反射状态机暂停推进时的 X 向累计误差有关，之后还需要补充偏航修正机制。

## 六、深沟跨越算法继续优化（2026-03-22）
今天继续在前一天的半闭环方案上往下调，主要处理三个问题：落地后的反弹、坑边踩空时反应偏慢，以及波浪步态运行一段时间后的偏航。当天结果目录为 `log/260322/19.30_learn`。

### 1. 落地原位锁定与起步软过渡
今天先调整了落地后的高度处理。程序在判断足端踩实后，不再立刻回到理论轨迹，而是先把当前高度锁在 `locked_z_val = cur_z`。这样做的目的是减少足端探底深度和真实地面高度之间的落差，避免刚落地就向上弹。除此之外，在摆动相起步时又加入了 15 帧平滑过渡，让足端从锁定高度逐步接回原来的轨迹，抬腿时的冲击比之前小了一些。

### 2. 异步短路探测窗口
探坑逻辑也做了修改。前一天的方案更偏向“等一等再确认”，今天改成了“先快判，再慢确认”。具体做法是把探测窗口拉长到 50 帧，但只要窗口内检测到足端实际高度低于 `0.98 m`，就立即判定为踩空并触发避险；如果一直没有出现下跌，再等到 50 帧结束后才把这次落脚记为踩实。这样处理后，坑边滑落时的反应更快，同时又保留了对误判的缓冲。

### 3. 闭环步幅不对称纠偏
针对偏航问题，今天没有再去直接扭机身姿态，而是尝试从步幅入手修正。程序每隔一段时间读取一次机体 Yaw 角，并根据偏差去调整左右两侧的步幅。如果机头偏左，就缩短一侧、放大另一侧，让机器人在后续几个周期里自己慢慢修回来。这个方法改动不大，但比直接改姿态更稳一些。

### 4. 重心前移与步态强制同步
另外还补了两个辅助处理。一个是当前机体越过深沟前段后，会把整体重心平滑向前移 0.20 m，主要是为了给后腿跨坑留出更稳定的受力条件。另一个是只要有腿还在探测或避险，主时钟 `kk` 就先暂停，不让理论步态继续往前走。这样能保证物理动作和数学轨迹尽量同步，减少节奏错位。

### 5. 后续待优化方向
今天的调整后，深沟跨越已经比前一天稳一些，但还有两件事没完全解决。第一，当前状态机还是“先退回，再大步跨越”，动作比较稳，但用时偏长，后面可以试试在部分情况下直接前跨，减少回退过程。第二，如果某条腿已经踩实，而另一条腿正好卡在坑内，现有流程还缺少专门的单腿脱困处理，后续还要补这一块。

## 七、深沟跨越控制继续修正与问题记录（2026-03-23）
今天继续围绕深沟场景调整半闭环脚本，重点不再是新增一个完全不同的控制框架，而是在现有状态机上继续往下收。主要改动集中在三块：一是把原先“先退回再跨越”的顺序改成“先直接前跨，失败后再退回并进行二次跨越”；二是在足端已经踩实但 X 坐标仍落在坑中区间时，增加了单腿脱困处理；三是把重心补偿从单段前移改成按前腿和中腿的确认情况分级推进。

### 1. 先直接前跨，失败后再退回
今天把深沟避险状态机重新整理了一遍。当前腿第一次踩空后，不再立刻执行后退，而是先尝试直接前跨。现在第一步直接前跨的步长改成了 `0.75 m`，如果这一脚还是没有踩实，再退回安全区，之后用 `0.85 m` 做第二次跨越。这样做的目的主要是减少一次不必要的回退，尽量在第一次反应里直接把腿送到对岸。

### 2. 单腿脱困从“本腿处理”扩展到“顺带检查其他腿”
前一天只考虑了“当前这条腿已经踩实，但仍落在坑中”的情况。今天又往前推进了一步，只要有某条腿踩实成功，程序就会顺带检查其他仍处于稳定支撑状态的腿。如果发现它们的 X 坐标仍落在 `1.5 ~ 2.2 m` 的坑中区间，就对这些腿触发较小步幅的单腿脱困。当前腿自己掉在坑中区间时，脱困补跨用 `0.55 m`；顺带检查到的其他腿，则只补 `0.30 m`，尽量减少对整体姿态的扰动。

### 3. 重心补偿改成前腿和中腿分级推进
重心补偿这部分今天也做了收束。当前逻辑不再是一旦进入过坑区间就统一前移，而是分成四个阶段：前腿都未确认时先后移 `0.15 m`；前腿未全部确认前先保持中性；两条前腿都确认后前移 `0.20 m`；两条中腿也确认后再补 `0.20 m`，总共达到 `0.40 m`。同时，为了避免重心阶段在中腿摆动和落地时来回切换，程序改成了一次过坑过程中只允许阶段向前推进，不再从阶段 3 回退到阶段 2。

### 4. 与 3 月 22 日结果的对比
为了更明确地判断这次修改有没有起作用，我把本次结果 `log/260323/15.35` 和前一天的 `log/260322/14.38_learn` 做了一个对比。两次都是深沟场景，虽然控制细节已经不完全一样，但从摘要数据和轨迹图来看，还是能看出几项比较明确的变化。

第一，动作链条明显缩短了。3 月 22 日那次总帧数为 `7601`，理论仿真时长 `38.005 s`；本次降到了 `6020` 帧和 `30.100 s`，分别减少了 `20.8%`。这一点和今天将恢复顺序改成“先直接前跨，失败后再退回”，以及把单腿脱困单独接入状态机有直接关系。原来一旦踩空就先退回，路径里会多出一段明显的往返动作；现在先尝试直接前跨，只有失败后才回撤，整体流程更短。

第二，路径冗余比前一天小了不少。3 月 22 日那次总路径长度是 `12.6362 m`，起终点直线位移是 `7.1039 m`，路径/位移比约为 `1.779`；本次总路径长度降到 `7.8498 m`，直线位移为 `5.7021 m`，路径/位移比约为 `1.377`，下降了 `22.6%`。虽然这次总位移本身也更短，但从这个比值来看，机器人在完成跨坑时多余的折返和横向绕动确实减少了。我认为这主要对应两项改动：一项是“先直接前跨，失败再退回”减少了无效回撤；另一项是踩实后补上的单腿脱困，让落在坑中区间内的腿可以尽快再送一步，不至于在坑边反复磨。

第三，速度峰值被压下来了，过程看起来更收一些。3 月 22 日那次峰值速度为 `1.3709 m/s`，本次降到了 `0.7603 m/s`，下降约 `44.5%`。从速度图上也能看出，前一天中后段存在比较明显的尖峰，而这次虽然速度还是有起伏，但尖峰数量和幅值都小了一些。这一变化应该和落地后的原位锁定、单腿脱困采用较小补跨，以及重心补偿改成分级推进有关。也就是说，这一轮不是单纯追求更快，而是先把动作压稳。

第四，偏航累积比前一天轻了。从两次轨迹图看，3 月 22 日那次机器人最终在世界坐标系 Y 方向上偏到了大约 `1.5 m`，而本次末段偏移大约在 `0.3 m` 左右，量级上明显小了一截。这一改善和步幅不对称纠偏继续保留，以及重心阶段切换改成单向推进有关系。前一天深沟中后段还能看到比较明显的横向漂移，本次虽然还没有完全压平，但至少没有再像前一次那样越走越偏。

第五，程序运行效率也有改善。前一天一轮仿真的程序实际运行时长是 `1067.083 s`，本次降到了 `284.444 s`，缩短了 `73.3%`，相当于加快了约 `3.75` 倍；如果按“实际运行时长 / 理论仿真时长”看，前一天约为 `28.1` 倍实时，本次约为 `9.4` 倍实时。不过这一点我暂时不打算下太绝对的结论，因为两次动作序列标签已经从 `ditch_learn` 收束到了 `ditch`，流程长度本身也不同，所以这里只能说明这次修改后整体仿真负担确实减轻了一些，但还不能说明速度问题已经彻底解决。

综合来看，这一轮修改最明显的进步不是“机器人突然走得更快了”，而是动作更短、路径更直接、偏航更小、速度尖峰也被压住了一些。这和这次几项改动的方向基本是一致的：直接前跨优先减少了无效折返，单腿脱困减少了坑边停滞，分级重心补偿和阶段锁存则让中后段推进更连贯。

### 5. 当前还没有解决的问题
这一轮改完后，深沟过坑动作比前几天更完整了，但还有几个问题还没有解决。

第一，仿真速度依然很慢。虽然理论动作本身只有几十秒，但一轮完整仿真往往要跑二十多分钟，调参效率比较低。

第二，最开始设想的重心后移并没有明显发挥作用。代码里虽然加上了前腿未确认时的后移阶段，但从实际效果看，机器人在刚靠近坑边时并没有明显把重心压住，这一部分还需要继续调。

第三，也是目前最关键的问题，是机器人伸腿入坑这件事还不够自然。现在很多时候，机器人只有在姿态已经有一点失稳时，腿才比较容易真正伸进坑里；如果机身状态过于稳定，足端反而不太容易下探到坑中。这说明目前伸腿动作和整机姿态之间的配合还不够理想，后续还要继续针对这一点做处理。
